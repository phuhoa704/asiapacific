export interface Step {
    id: number
    title: string
    descr: string
}

export interface Office {
    id: number
    icon: string
    location: string
    address: string
}

export interface News {
    id: number
    image: string
    date: string
    title: string
    descr: string
}

export interface Region {
    id: number
    image: string
    location: string
    descr: string
}